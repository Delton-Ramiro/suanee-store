// API client utilities — placeholder
// Will implement typed fetch wrappers for all endpoints once web/admin UIs are built

export const API_BASE =
  process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001/api/v1";

export {};
