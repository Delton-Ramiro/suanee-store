export default function EncomendaDetailPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  return (
    <div className="py-12">
      <p className="text-text-muted text-sm">
        Detalhe da encomenda — em construção
      </p>
    </div>
  );
}
