export default function ColecaoPage({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  return (
    <div className="container-web py-12">
      <p className="text-text-muted text-sm">Coleção — em construção</p>
    </div>
  );
}
